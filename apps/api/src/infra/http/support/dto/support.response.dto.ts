import { SupportResource } from "@/modules/@shared/domain/enums";

/** Ecoa só o que foi contado. Não há nada de pessoal para devolver. */
export class SupportOpenedResponseDto {
  resource: SupportResource;
}
